import Link from "next/link"
import React from "react"

const Logo = () => {
  return (
    <div>
      {/* Replace with Logo */}
      <Link href="/" className="ease duration-900 flex items-center justify-center rounded-full border  border-white p-3 text-2xl font-bold text-white transition-[colors_transform] hover:scale-110 hover:bg-pink-400">
        CE
      </Link>
    </div>
  )
}

export default Logo
